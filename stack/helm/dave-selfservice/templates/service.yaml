apiVersion: v1
kind: Service
metadata:
  name: selfservice
  labels:
    {{- include "daveSelfService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol }}
      name: {{ .name }}
    {{- end }}

  selector:
    {{- include "daveSelfService.selectorLabels" . | nindent 4 }}
